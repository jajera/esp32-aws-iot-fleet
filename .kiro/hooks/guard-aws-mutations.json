{
  "version": "v1",
  "hooks": [
    {
      "name": "Guard AWS mutations",
      "description": "Blocks mutating AWS CLI and IaC from shell tools. Read-only calls pass. Opt in with FLEET_ALLOW_AWS=1. See .kiro/steering/lab-safety.md.",
      "trigger": "PreToolUse",
      "matcher": "(?i)(bash|shell|terminal|command|execute|process)",
      "action": {
        "type": "command",
        "command": "python3 .kiro/hooks/guard-aws-mutations.py"
      },
      "timeout": 10,
      "enabled": true
    }
  ]
}
